{{> header}}


{{#if isLoggedIn}}
{{!-- User page with collection --}}
<section id="viewCatalog" class="background-img">
    {{#each destinations}}
    <div class="added-destinations">        
        <a href="#/details/{{ id }}" class="added-destination">
            <img src="{{ this.imgUrl }}" alt="" class="picture-added-destination">
            <h3>{{this.destination}}</h3>
            <span>to {{this.city}} </span><span>{{ this.departureDate }}</span>
        </a>
    </div>
    {{else}}
    {{!-- User page without collection --}}
    <section id="viewCatalog" class="background-img">
        <div class="added-destinations">
        </div>
    </section>
    {{/each}}

</section>
{{else}}
{{!-- Guest page --}}
<section id="viewCatalog" class="background-img">
    <div class="added-destinations">
    </div>
    <div class="guest">
        No destinations possible! Please sign in...
    </div>
</section>
{{/if}}

{{> footer}}